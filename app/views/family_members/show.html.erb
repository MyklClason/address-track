<%- model_class = FamilyMember -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:user_id) %>:</strong></dt>
  <dd><%= @family_member.user_id %></dd>
  <dt><strong><%= model_class.human_attribute_name(:family_member_id) %>:</strong></dt>
  <dd><%= @family_member.family_member_id %></dd>
</dl>

<%= link_to t('.back', :default => t("helpers.links.back")),
              family_members_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_family_member_path(@family_member), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              family_member_path(@family_member),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>

<%= form_for(@alternative_email) do |f| %>
  <% if @alternative_email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alternative_email.errors.count, "error") %> prohibited this alternative_email from being saved:</h2>

      <ul>
      <% @alternative_email.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
